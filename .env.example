PORT=8082
HOST=127.0.0.1
LOG_LEVEL=info

# fs 工具(read/write/edit/bash/grep/list)的工作区根目录。
# 必须显式指定一个项目目录;留空则不注入 fs 工具。
# 不允许设为 $HOME 或 /(会被拒绝)。
TARGET_REPO_ROOT=

# 本地 SQLite 数据库路径;留空用 ~/.eva/eva.db
DB_PATH=